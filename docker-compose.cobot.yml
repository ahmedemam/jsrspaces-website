# Cobot - Coworking Space Management Platform
# Self-hosted integration via reverse proxy to Cobot SaaS
#
# Cobot uses SaaS - no Docker containers needed
# Routing is handled by proxy (see proxy/conf/Caddyfile)
# See COBOT-SETUP.md for configuration steps

services: {}

# Self-hosted Cobot (when available)
# When Docker image becomes available, uncomment below:
  
# cobot_db:
  #   image: postgres:15-alpine
  #   restart: unless-stopped
  #   container_name: jsrspaces_cobot_db
  #   environment:
  #     - POSTGRES_USER=cobot
  #     - POSTGRES_PASSWORD=${COBOT_DB_PASSWORD:-change_me}
  #     - POSTGRES_DB=cobot
  #   volumes:
  #     - jsrspaces_cobot_db:/var/lib/postgresql/data
  #   networks:
  #     - jsrspaces_network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U cobot"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # cobot:
  #   image: cobot/cobot:latest
  #   restart: unless-stopped
  #   container_name: jsrspaces_cobot
  #   depends_on:
  #     cobot_db:
  #       condition: service_healthy
  #   environment:
  #     - DATABASE_URL=postgresql://cobot:${COBOT_DB_PASSWORD:-change_me}@cobot_db:5432/cobot
  #     - SECRET_KEY_BASE=${COBOT_SECRET_KEY:-change_me}
  #     - RAILS_ENV=production
  #   volumes:
  #     - jsrspaces_cobot_data:/app/storage
  #     - jsrspaces_cobot_logs:/app/log
  #   networks:
  #     - jsrspaces_network
